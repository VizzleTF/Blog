---
import { getCollection } from "astro:content";
import PageLayout from "@layouts/PageLayout.astro";
import ArrowCard from "@components/ArrowCard";
import StackCard from "@components/StackCard.astro";
import { SITE, SOCIALS } from "@consts";

const posts = (await getCollection("blog"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 3);

const projects = (await getCollection("projects"))
  .filter((project) => !project.data.draft)
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 3);

const stack = [
  {
    text: "Astro",
    icon: "astro",
    href: "https://astro.build",
  },
  {
    text: "Javascript",
    icon: "javascript",
    href: "https://www.javascript.com",
  },
  {
    text: "Typescript",
    icon: "typescript",
    href: "https://www.typescriptlang.org",
  },
  {
    text: "Tailwind",
    icon: "tailwind",
    href: "https://tailwindcss.com",
  },
];
---

<PageLayout title="Home" description={SITE.DESCRIPTION}>
  <!-- HERO -->
  <section class="relative h-screen w-full">
    <div
      id="planetcont"
      class="animate absolute inset-0 top-1/4 overflow-hidden"
    >
      <div
        id="crescent"
        class="absolute top-0 left-1/2 -translate-x-1/2 w-[250vw] min-h-[100vh] aspect-square rounded-full p-[1px] bg-gradient-to-b from-black/25 dark:from-white/75 from-0% to-transparent to-5%"
      >
        <div
          id="planet"
          class="w-full h-full bg-white dark:bg-black rounded-full p-[1px] overflow-hidden flex justify-center"
        >
          <div
            id="blur"
            class="w-full h-20 rounded-full bg-neutral-900/25 dark:bg-white/25 blur-3xl"
          >
          </div>
        </div>
      </div>
    </div>
    <div
      class="animate absolute h-full w-full flex items-center justify-center"
    >
      <div class="relative w-full h-full flex items-center justify-center">
        <div class="p-5 text-center">
          <h1
            class="animated text-2xl md:text-3xl lg:text-4xl font-bold uppercase text-black dark:text-white"
          >
            Сказочный девопес
          </h1>
          <div
            id="ctaButtons"
            class="animated flex flex-wrap gap-4 justify-center mt-5"
          >
            <a
              href="/blog"
              class="py-2 px-4 rounded truncate text-xs md:text-sm lg:text-base bg-black dark:bg-white text-white dark:text-black hover:opacity-75 blend"
            >
              Читать блог
            </a>
          </div>
        </div>
      </div>
    </div>
    <div id="scroll-chevron" class="absolute bottom-8 left-1/2 -translate-x-1/2 animate transition-opacity duration-300">
      <a
        href="#content"
        aria-label="Прокрутить вниз"
        class="flex items-center justify-center w-20 h-10 text-black/50 dark:text-white/50 hover:text-black dark:hover:text-white transition-colors duration-300 scroll-chevron"
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="64"
          height="32"
          viewBox="0 0 48 24"
          fill="none"
          stroke="currentColor"
          stroke-width="1.5"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <polyline points="0 9 24 15 48 9"></polyline>
        </svg>
      </a>
    </div>
  </section>

  <div id="content" class="relative bg-white dark:bg-black">
    <div class="mx-auto max-w-screen-sm p-5 space-y-24 pb-16">
      <!-- Blog Preview Section -->
      <section class="animate">
        <div>
          <div class="flex items-baseline justify-between mb-2">
            <h2 class="text-base font-semibold text-black dark:text-white tracking-tight">
              Последние посты
            </h2>
            <a
              href="/blog"
              class="group text-sm text-dim hover:text-accent transition-colors"
            >
              Все посты →
            </a>
          </div>
          <ul class="flex flex-col border-t border-black/10 dark:border-white/15">
            {
              posts.map((post) => (
                <li>
                  <ArrowCard entry={post} />
                </li>
              ))
            }
          </ul>
        </div>
      </section>

      <!-- QR Code Section -->
      <section class="animate">
        <div class="flex flex-col items-center gap-3 mt-8">
          <img
            src="/telegram.png"
            alt="QR код для быстрого доступа к Telegram каналу Сказочный девопес"
            class="max-w-xs rounded-lg shadow-md"
          />
          <a
            href="https://t.me/gemini_commit"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-1.5 font-mono text-xs text-accent font-semibold hover:underline underline-offset-4"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="14"
              height="14"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.8"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M21 4L2.5 11.5l6 2.2 2.2 6.3L15 14l4.5 4L21 4z"></path>
              <path d="M8.5 13.7L21 4l-9.5 10"></path>
            </svg>
            t.me/gemini_commit
          </a>
          <div class="text-xs text-dim">
            Канал в Telegram — короткие заметки
          </div>
        </div>
      </section>
    </div>
  </div>
  <script is:inline>
    document.addEventListener("DOMContentLoaded", function () {
      var chevron = document.getElementById("scroll-chevron");
      window.addEventListener("scroll", function () {
        if (window.scrollY > 50) {
          chevron.style.opacity = "0";
          chevron.style.pointerEvents = "none";
        } else {
          chevron.style.opacity = "1";
          chevron.style.pointerEvents = "auto";
        }
      }, { passive: true });
    });
  </script>
</PageLayout>
